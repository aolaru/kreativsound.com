# Retired App Sources

This folder keeps old prototype sources that should not be copied into the public Astro build.

- `audio-alchemy` keeps pre-rename Audio Alchemy output and notes. The live public path now redirects to Preset Mutator Audio to Preset.
- `presetmutator` keeps the older prototype source tree. The live public path now redirects to `apps/preset-mutator`.

Keep only redirect stubs under `apps/` when an old public URL still needs to resolve.
